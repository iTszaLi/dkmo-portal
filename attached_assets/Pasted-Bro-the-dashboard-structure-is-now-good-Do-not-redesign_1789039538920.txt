Bro, the dashboard structure is now good. Do not redesign it again. I only want you to fix the following issues and keep everything else as it is. 1. FIX TOTAL MEMBERS — CRITICAL The dashboard currently shows: Total Members: 0 This is incorrect. The Members module currently contains the migrated DKMO members, so the dashboard must calculate Total Members directly from the real members database. The dashboard should show the actual current member count, which is currently expected to be 1,140 based on the completed migration. Do NOT hardcode 1,140. Query the database so the number automatically stays synchronized with the Members module. 2. FIX ACTIVE MEMBERS — CRITICAL The dashboard currently shows: Active Members: 0 This is also incorrect. Calculate Active Members from the actual member status in the database. It must use the same status logic as the Members module. Do not create a separate dashboard calculation that can become inconsistent. 3. REMOVE “MEMBERS WITH MISSING DATA” FROM ATTENTION REQUIRED Currently Attention Required shows: Members with missing data — 81 members have empty required fields. Remove this from the main dashboard. The migrated historical Access database naturally contains some incomplete/blank fields, and this is not necessarily an urgent committee action. If a data-quality checker already exists, keep it inside the Members/admin area, but do not present it as a dashboard warning. The main dashboard should focus only on actions that genuinely require committee/admin attention. 4. VERIFY MEMBERSHIP PAYMENTS PENDING The dashboard currently shows: 1 member needs membership payment review Verify this number against the actual Membership Fees/payment ledger. Do NOT simply make it zero. If there is genuinely one member whose membership fee is unpaid/pending, keep 1. If the payment ledger says the member is already paid, fix the calculation so the dashboard correctly shows zero. The dashboard and Payments → Membership Fees must use the same source of truth. 5. KEEP THE CURRENT DASHBOARD STRUCTURE Do NOT add more cards. Keep: Core Overview Total Members Active Members Membership Fees Pending Monthly Collections FRF Overview Pending FRF Claims Active FRF Case FRF Outstanding Attention Required Only genuinely actionable items. 6. KEEP THE FRF LOGIC EXACTLY AS IMPLEMENTED Remember: Pending FRF Claims = claims awaiting review Active FRF Case = maximum 1 active case FRF Outstanding = amount still due for the current active case If there is no active FRF case → Active FRF Case = 0 and FRF Outstanding = SAR 0.00 Membership Paid → FRF Active/eligible Membership Unpaid → FRF Inactive/not eligible Do not create duplicate or conflicting FRF calculations. 7. IMPORTANT — DATABASE SOURCE OF TRUTH Every dashboard number must be derived from the same underlying database records used by the respective module. For example: Total Members → Members database Active Members → Members/status database Membership Pending → Membership payment ledger Monthly Collections → Payment records Pending FRF Claims → FRF Claims Active FRF Case → FRF case records FRF Outstanding → FRF Fees for the active case Do not hardcode dashboard numbers. FINAL TEST After fixing this, verify that: Dashboard Total Members matches Members module Dashboard Active Members matches Members module Dashboard Membership Pending matches Payments → Membership Fees Dashboard Monthly Collections matches Payments Dashboard Pending FRF Claims matches FRF Claims Dashboard Active FRF Case matches FRF Claims Dashboard FRF Outstanding matches Payments → FRF Fees No dashboard metric is using stale/sample data The dashboard design is now good. Only fix the data accuracy and remove the unnecessary missing-data warning. Do not make additional cosmetic changes.Bro, make one final change to the Dashboard finance section. REMOVE “MONTHLY COLLECTIONS” Remove: Monthly Collections “Total payments collected this month” I do not want monthly collections on the main dashboard. REPLACE IT WITH “TOTAL MEMBERSHIP FEES COLLECTED” Add: Total Membership Fees Collected Show the total historical amount of membership fees actually collected/paid by members. Example: SAR XX,XXX.00 This must be calculated from the actual Membership Fees payment ledger. IMPORTANT: Count only membership payments that are actually Paid/collected. Do not include unpaid/pending membership fees. Do not include FRF payments. Do not include loan payments. Do not hardcode the amount. It must automatically update whenever a membership payment is recorded, edited, or corrected. Subtitle: “Total membership fees received” ADD “LOAN MONEY OUTSTANDING” Add another core financial metric: Loan Money Outstanding Show the total amount of money currently owed to DKMO from member loans. This should be calculated from the actual Loan module. The calculation should represent: Total loan amount disbursed − total principal repaid = current loan outstanding Only outstanding principal should be counted. Do not include: Fully repaid loans Cancelled/rejected loan requests Amounts that were never disbursed If there are no outstanding loans, show: SAR 0.00 Subtitle: “Outstanding member loan balance” Make the card clickable and take the admin to the Loan module. UPDATED CORE OVERVIEW The Core Overview should now be: Total Members Active Members Membership Fees Pending Total Membership Fees Collected Loan Money Outstanding Keep the existing FRF Overview separately: Pending FRF Claims Active FRF Case FRF Outstanding Do not add Monthly Collections back. IMPORTANT All these numbers must come from the actual database/modules and must remain synchronized. Do not hardcode any figures or create separate dashboard-only financial calculations.